
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
# monitor=,highres,auto,auto
monitor=eDP-1,2560x1600@240,0x0,1
# monitor=eDP-1,1920x1080@60,0x0,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty 
$fileManager = thunar
$menu = tofi-run | xargs hyprctl dispatch exec --
$lock = hyprlock


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
exec-once = waybar & fcitx5
exec-once = hypridle
exec-once = swaybg --output "*" --mode fill --image /home/spikely/spk/my-nixos/home/desktop/wallpaper1.jpg
exec-once = systemctl --user start hyprpolkitagent
# exec-once = waybar & fcitx5
# exec-once = mpvpaper -o "no-audio --loop-playlist shuffle" '*' /home/spikely/spk/mpvpaper/1_1.mp4
# exec-once = mpvpaper -o "no-audio --loop-playlist shuffle" '*' /home/spikely/spk/mpvpaper/sky_loop.mp4

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = MOZ_ENABLE_WAYLAND,1
env = GDK_SCALE,2

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 1                    # 窗口之间的间隙大小
    gaps_out = 2                  # 窗口与显示器边缘的间隙大小

    no_border_on_floating = false  # 是否禁用浮动窗口的边框
    gaps_workspaces = 2          # 工作区之间的间隙
    border_size = 2               # 窗口边框的宽度

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # 活动窗口的边框颜色
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # 非活动窗口的边框颜色
    col.inactive_border = rgba(595959aa)

    col.nogroup_border_active = 0xffffaaff        # 无法添加到组的窗口的活动边框颜色
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    # resize_on_border = false
    resize_on_border = true

    # 扩展边框周围的区域 
    extend_border_grab_area = 15

    # 将鼠标悬停在边框上时显示光标图案，仅在resize_on_border 设置为真时生效
    hover_icon_on_border = true
    # 焦点移动到未找到窗口的方向时，是否回退到下一个可用窗口
    no_focus_fallback = false
    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    # 调整大小时强制浮动窗口使用特定角（1-4从左上角顺时针方向）
    resize_corner = 3
    layout = dwindle
    # layout = master

    # 浮动窗口对齐
    snap {
         enabled = false    # 浮动窗口对齐总开关
         window_gap = 10
         monitor_gap = 10
         border_overlap = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 12         #圆角大小

    # Change transparency of focused and unfocused windows
    # 活动窗口的不透明度
    active_opacity = 1.0
    # 非活动窗口的不透明度
    inactive_opacity = 1.0

    # 全屏窗口的不透明度
    fullscreen_opacity = 1.0

    # 启用非活动窗口的调光，这里如果启用的话，输入法窗口似乎会被当作非活动窗口
    dim_inactive = false
    # 应该将非活动窗口变暗多少，取值为0.0-1.0
    dim_strength = 0.2
    # 打开特殊工作区时，屏幕其余部分的调光量
    # dim_around = 0.4 # 窗口规则应变暗多少
    
    dim_special = 0.2
    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 5
        passes = 1
        new_optimizations = true
        
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        # 键入时禁用触摸板
        disable_while_typing = true
        # 反转滚动方向，启用后，直接移动内容而不是操纵滚动条
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    # 在触摸板启用工作区滑动手势
    workspace_swipe = true
    # 设置触摸板手势的手指数
    workspace_swipe_fingers = 3
    # workspace_swipe = false
    # 滑动是否不限制相邻的工作区，而是继续缓动到下一个工作区
    workspace_swipe_forever = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

# 工作组相关配置，暂时不清楚具体作用，先注释
# group {
#       auto_group = true
#       groupbar {
#                enabled = true
#       }     
# }

# https://wiki.hyprland.org/Configuring/Variables/#misc
###################
####### MISC ######
###################
misc {
     # 禁用随机hyprland log以及背景
     disable_hyprland_logo = true
#    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
     vfr = true
     # 控制显示器的自适应同步
     vrr = 0
     # 是否关闭配置文件保存时自动加载
     disable_autoreload = false
}

# 打开x11，允许运行x11应用
xwayland {
     enabled = true
     force_zero_scaling = true
}

cursor {
     sync_gsettings_theme = true
     # 设置鼠标在20秒处于非活动状态后隐藏
     inactive_timeout = 50
}

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
bind = $mainMod SHIFT, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, F, fullscreen
bind = $mainMod, V, togglefloating,
bind = $mainMod, D, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# hyprlock
bind = $mainMod SHIFT, L, exec, $lock # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# move window
bind = $mainMod, H, movewindow, l
bind = $mainMod, L, movewindow, r
bind = $mainMod, J, movewindow, d
bind = $mainMod, K, movewindow, u
 

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 5%+ && notify-send "Brightness - $(brightnessctl info | awk 'NR == 2 {gsub(/[()]/, "", $4); print $4}')"
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 5%- && notify-send "Brightness - $(brightnessctl info | awk 'NR == 2 {gsub(/[()]/, "", $4); print $4}')"

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

bind=$mainMod SHIFT, T,exec,XDG_CURRENT_DESKTOP=sway flameshot gui --raw -p ~/Pictures/Screenshots | wl-copy
# screenshot
bind = $mainMod SHIFT, P, exec, hyprshot -m region --clipboard-only
bind = $mainMod SHIFT, W, exec, hyprshot -m window --clipboard-only

# 快捷启动某些应用
bind = $mainMod SHIFT, Z, exec, emacs
bind = $mainMod SHIFT, F, exec, thunar
bind = $mainMod SHIFT, E, exec, microsoft-edge

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
